# README 与技能命名调整 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 增加面向中文读者的仓库 README，并将腾讯会议技能名称统一为 `itriton-skills-tmeet-assistant`。

**Architecture:** 根目录 `README.md` 作为仓库入口，只提供项目概览、安装、快速使用和维护信息，完整命令文档继续由技能目录维护。技能身份仅通过 `SKILL.md` frontmatter 的 `name` 字段调整，不改变现有行为。

**Tech Stack:** Markdown、YAML frontmatter、Python `unittest`、Git

---

### Task 1: 更正技能名称

**Files:**
- Modify: `tmeet-assistant/SKILL.md:2`

- [ ] **Step 1: 验证旧名称存在**

Run: `rg -n "ixhlink-skills|itriton-skills" tmeet-assistant/SKILL.md`

Expected: 第 2 行显示 `name: ixhlink-skills-tmeet-assistant`。

- [ ] **Step 2: 修改技能名称**

将 frontmatter 更新为：

```yaml
name: itriton-skills-tmeet-assistant
```

- [ ] **Step 3: 验证旧品牌已清除**

Run: `rg -n "ixhlink-skills" . --glob '!docs/superpowers/**'`

Expected: 无输出，退出码为 1。

### Task 2: 创建中文 README

**Files:**
- Create: `README.md`

- [ ] **Step 1: 编写 README**

README 依次包含：项目简介、核心能力、环境要求、安装、快速使用、目录结构、测试和安全提示。安装命令使用仓库 HTTPS 地址，技能详情使用相对链接 `tmeet-assistant/SKILL.md`。

- [ ] **Step 2: 校验 README 链接目标**

Run: `test -f tmeet-assistant/SKILL.md && test -f tmeet-assistant/scripts/calculate_attendance.py && test -d tmeet-assistant/references && test -d tmeet-assistant/tests`

Expected: 无输出，退出码为 0。

- [ ] **Step 3: 校验 Markdown 格式**

Run: `git diff --check`

Expected: 无输出，退出码为 0。

### Task 3: 回归验证与交付

**Files:**
- Test: `tmeet-assistant/tests/test_calculate_attendance.py`

- [ ] **Step 1: 运行完整测试**

Run: `python3 -m unittest discover -s tmeet-assistant/tests -v`

Expected: `Ran 13 tests` 和 `OK`。

- [ ] **Step 2: 检查最终差异**

Run: `git diff -- README.md tmeet-assistant/SKILL.md`

Expected: 只有 README 新增和技能名称变更。

- [ ] **Step 3: 提交实现**

```bash
git add README.md tmeet-assistant/SKILL.md docs/superpowers/plans/2026-07-22-readme.md
git commit -m "docs: add Chinese project readme"
```

- [ ] **Step 4: 推送并核对远端**

Run: `git push origin main`

Expected: `main -> main`，随后 `git status --short --branch` 显示本地与 `origin/main` 同步。
